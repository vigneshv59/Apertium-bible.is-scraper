# ApertiumBibleScraper

1. Run `bundle install`
2. Run `bundle exec ruby scraper.rb -u "<put url here>" -o "<put output file here>" -n <verse limit> -t <time limit (integer)>`
3. Output will be at `output.txt`
